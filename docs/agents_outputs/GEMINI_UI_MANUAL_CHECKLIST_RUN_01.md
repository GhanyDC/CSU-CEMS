# UI Manual Validation Checklist Run 01

## Admin Page Visual Checks
- [ ] Admin panel headers and page titles correspond to the new robust serif/sans-serif sizes.
- [ ] "Admin Portal" secondary actions correctly use the Gold/Amber `#F2A900` accent color.

## Election List Page Checks
- [ ] List page table styling or card formats reflect a high-contrast white background over `#F8F9FB`.
- [ ] "Manage >" and creation buttons properly adapt hover styling mapped from `--cems-primary-light` (`#A11217`).

## Election Detail Page Checks
- [ ] Main primary action button ("Start Election") is deeply maroon colored with a play icon.
- [ ] Tab navigation borders use a subtle gray (`#E5E7EB`) without excessive shadow.
- [ ] Status badges for "Draft", "Campus Election" etc. properly pop against the light theme.

## Tab Consistency Checks
- [ ] Active tabs display a firm under-border or full contrasting background (White on Gray) vs. inactive tags seamlessly matching mockups.
- [ ] Spacing within tabs has clear padding/margin separations to mimic the screenshot.

## Student Login/Dashboard Visual Checks
- [ ] Unified background color and shadow logic as presented in the admin pages.
- [ ] The Student Login page correctly implements the logo’s maroon identity (`#6B0F1A`) exclusively (no blue default templates).

## Ballot/Results Visual Checks
- [ ] Student selection toggles or checkboxes share the unified red/maroon or gold select states.
- [ ] Published final UI is identical to the admin preview aesthetics regarding typographic scale.

## Responsive Checks
- [ ] Mobile/Tablet views appropriately stack the cards without horizontal scrolling (except expected tables).
- [ ] Nav headers don't aggressively wrap titles.

## Interaction Checks
- [ ] All primary buttons correctly display a `:hover`, `:focus`, or `:active` state derived from the new CSS variables (`--cems-primary-light` and `--cems-primary-dark`).

## Expected Outcomes
The result is a holistic visually unified system utilizing a highly distinctive Maroon and Yellow signature tone for both students and admins, mirroring the exact aesthetics and layouts designated by the original screenshots.
